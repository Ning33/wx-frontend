<view class="page">
  <view class="info">
    <view class="info_">
      <text>姓名:</text>
      <text class="letter">{{personInfo.name}}</text>
    </view>
    <view class="info_">
      <text>性别:</text>
      <text class="letter">{{personInfo.sex}}</text>
    </view>
    <view class="info_">
      <text >出生年月:</text>
      <text class="letter">{{personInfo.birthday}}</text>
    </view>
    <view class="info_">
      <text>身份证号码:</text>
      <text class="letter">{{personInfo.idcard}}</text>
    </view>
    <view class="info_">
      <text>住址:</text>
      <text class="letter">{{personInfo.address}}</text>
    </view>
    <view class="info_">
      <text>电话号码:</text>
      <text class="letter">{{personInfo.tel}}</text>
    </view>
  </view>
------------------------------------------------------------
  <hnisi-picker mode="multiSelector" range="{{multArray}}" value="{{multValue}}" label="地区"  range-key="{{'name'}}"
   bindchange="handleChange" bindcancel="handleTouchCancel" bindcolumnchange="handleColumnChange" >
      <!-- {{array[index].dictDisplay}} -->
      {{multArray[0][multIndex[0]].name}} {{multArray[1][multIndex[1]].name}}
    </hnisi-picker>
</view>
